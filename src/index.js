import React from "react";
import ReactDOM from "react-dom/client";
import MvMouse from "./MvMouse";
import "./MvMouse.css";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MvMouse />
  </React.StrictMode>
);
